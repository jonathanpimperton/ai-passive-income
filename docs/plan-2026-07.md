# CalcRun Execution Plan — July 2026 (LIVING DOCUMENT)

> **Purpose:** Full plan + live progress tracker for the July 2026 overhaul. If a session
> ends mid-work, a new session resumes from the **Session Handoff State** section below.
> Keep this document updated as tasks complete. Companion doc: `docs/review-2026-07.md`
> (the full 120-finding review this plan came from).

Last updated: 2026-07-02 (Phase 1 in progress)

---

## Owner Decisions (locked 2 July 2026)

| # | Decision | Choice |
|---|---|---|
| 1 | Design direction | **B — "Precision Instrument"**: one grotesk typeface + JetBrains Mono for ALL financial figures (brand signature), ink/paper near-monochrome palette with deep teal as sole accent, 1px borders not drop shadows, 6-8px radii, custom flat chart system, compact light page headers |
| 2 | Doorway scenarios (~70 numeric variants) | **Consolidate into hub pages + 301s** (not rewrite-in-place) |
| 3 | Analytics | **GA4 behind Consent Mode v2 default-denied** (keep GA4 + event taxonomy) |
| 4 | Untracked affiliate cards | **Keep showing** (do not hide pending partners) |
| 5 | Dev/file tools in sitemap | **Keep** (no sitemap exclusion) |
| 6 | Display ads | **No** (preserves the no-ads differentiator) |
| 7 | Distribution + revenue work | **Deferred by owner** — focus is getting the website right first. Skip Phase 4 (revenue plumbing) and all owner-distribution tasks for now |
| 8 | Slider precision | Owner explicitly flagged sliders hard to use precisely, especially mobile — treated as first-class Phase 1+2 item |

Git policy (owner-approved 2 Jul 2026): **local checkpoint commits at milestones are OK;
HOLD PUSH until owner reviews the design phase** (push auto-deploys via Cloudflare).
Docs committed in 65e3a8a; code checkpoint follows when Phase 1 agents finish + tests pass.

---

## PHASE 1 — Correctness & Trust Emergency

The site's core claim ("verified rates") must be true before anything else.

### 1A. Tax data 2026/27 (UK) + 2026 (US) — ✅ DONE
- [x] Researched + adversarially verified all rates from primary sources (6-agent workflow,
      GOV.UK / gov.scot / IRS / Ofgem). Full sourced digests in session scratchpad; key
      deltas recorded below.
- [x] `src/lib/uk-rates.ts` → 2026/27: student loans Plan 1 £26,900 / Plan 2 £29,385 /
      Plan 4 £33,795 (Plan 5 £25,000 frozen, first repayments Apr 2026); state pension
      £241.30/wk; Scottish bands corrected (old values were 2024/25!) and updated to
      2026/27 (Basic from £3,967, Intermediate from £16,956 taxable). UNCHANGED (verified):
      PA £12,570 (frozen to 2031), rUK bands, NI 8%/2% (PT £12,570 / UEL £50,270),
      employer NI 15%/£5,000, SDLT (post-Apr-2025 reversion), CGT £3,000 + 18%/24%.
- [x] `src/lib/us-rates.ts` → 2026 (Rev. Proc. 2025-32, post-OBBBA): brackets
      single/married/head (single 12% from $12,400, 22% from $50,400; HoH verified
      separately vs Tax Foundation), standard deduction $16,100/$32,200/$24,150,
      SS wage base $184,500, LTCG brackets ($49,450/$545,500 single), 401(k) $24,500
      (catch-up $8,000).
- [x] `src/lib/tax-rates.test.ts` rewritten — 15 tests green, incl. recomputed $75K
      end-to-end ($7,670 federal) and Scottish band pins.
- [x] Authoritative take-home tables generated with the site's own engines →
      `docs/tax-values-2026.json` (UK £20K-£200K, US $40K-$250K single; incl. Plan 2/5
      student-loan amounts). Regenerate anytime by importing calcIncomeTax/calcNI/
      calcFederalTax/calcFICA in a temp vitest file.
- Research facts for content writers: Autumn Budget 2025 salary-sacrifice NI cap starts
  **April 2029** (not 2026/27); dividend rates +2pts from Apr 2026 (10.75/35.75/39.35);
  pension-into-IHT is **legislated** (FA 2026) effective deaths on/after 6 Apr 2027;
  Cash ISA cut to £12,000 (under-65s) is **April 2027**, 2026/27 ISA still £20,000 any-mix;
  child benefit £27.05/£17.90, HICBC £60K-£80K unchanged; SDLT unchanged; BADR now 18%;
  SEG guidance ~13p/kWh; Ofgem cap Jul-Sep 2026 electricity 26.11p/kWh.

### 1B. Verified bug fixes — ✅ DONE
- [x] `calculator-utils.ts` debtPayoff: freed minimums now roll permanently into the pool
      (was: 20 months on a case that should be 13). 2 new pinning tests; 1 old test that
      encoded the bug corrected (27 → 23 months).
- [x] `DecisionEngine.tsx` solar tab: electricity-bill slider was a no-op; now drives a
      usage-derived self-consumption curve (responds across the whole range).
- [x] `SliderInput.tsx`: keyboard support on non-linear sliders (WCAG 2.1.1 fix);
      `textMin`/`textMax` props (slider = realistic range, text keeps old ceiling);
      neutral "Adjusted to maximum..." notice replaces contradictory clamp error;
      `aria-describedby` wired; non-linear auto-detect threshold 1000 → 250 steps.
- [x] `pension-vs-isa-uk.md`: pension access age 55 (57 from Apr 2028) — was wrong both
      numbers; IHT row updated to legislated Apr 2027 change.
- [x] GA4 Consent Mode v2: default denied, honors stored consent, Accept upgrades;
      moved from first-in-head to end-of-head. `CookieConsent.tsx` fires consent updates.
- [x] `worker.ts`: Turnstile fails closed (503 if secret unset); MailerLite non-422
      failures now throw → real 502 to UI (was silent fake success).

### 1C. Slider recalibration (all calculators) — ✅ DONE (agent)
- [x] 22 calculator components: slider max cut to realistic P95 (e.g. compound start
      $10M → $500K slider, textMax $10M), steps sized for the non-linear mapping,
      minLabel/maxLabel endpoints on every money slider. Full per-file table in the
      agent report (see git diff). 236 tests green.

### 1D. Trailing-slash + hygiene sweep — ✅ DONE (agent)
- [x] `trailingSlash: 'always'`; 201 link fixes across 134 files (incl. getToolPath at
      source, breadcrumb JSON-LD URLs, embed iframe URL, worker email links, Playwright
      specs); scanner verifies 0 slash-less internal links remain in src
- [x] Methodology pages de-orphaned: "How we calculate this →" pill on financial
      calculator pages (renders only when the methodology exists)
- [x] BreadcrumbList JSON-LD deduped on scenario/comparison/how-we-calculate templates
- [x] `public/_headers`: /_astro/* + /fonts/* 1y immutable, /og/* 1 day
- [x] Sitemap lastmod from real tool frontmatter dates (10 pages; no fake freshness)
- [x] Deps: mammoth/html2canvas/html2pdf.js removed, docx → devDeps (used by test
      fixtures), @types/typescript/@astrojs-check → devDeps, html2pdf shim deleted;
      `npm run check` = `tsc --noEmit` (19s; `check:astro` kept but OOMs — pre-existing)
- [x] BONUS BUG FOUND+FIXED: results-email CTA links were `/tools/{slug}/` (missing
      category segment — every email link 404'd). Added TOOL_PATHS map in worker.ts.
- KNOWN DEBT: 22 pre-existing tsc errors in unrelated files (pdfjs-dist v5 types, satori
  Buffer casts, worker.ts missing @cloudflare/workers-types, playwright type imports) —
  follow-up item, not introduced by this work.

### 1E. Math extraction + tests for the 4 new calculators — ✅ DONE (agent)
- [x] src/lib/stamp-duty.ts, capital-gains.ts, car-finance.ts (reuses
      loanMonthlyPayment), solar-payback.ts — inline tables deleted from components,
      all now driven by central rates modules
- [x] BUG FOUND+FIXED: US long-term CGT band overflow — gains spanning all three LTCG
      brackets were overtaxed by 15% x $49,450 = $7,417.50 (15% band swallowed the 0%
      band's share). Fixed in lib with regression test.
- [x] 62 new hand-verified unit tests (SDLT £600K = £20,000 total, FTB £300K = £0,
      FTB £550K loses relief = £17,500, UK CGT £1,260, car loan £386.66/mo, etc.)
- [x] SalaryUkCalc/SalaryCalc pills render {UK_TAX_YEAR}/{US_TAX_YEAR}; 401(k) cap now
      US_401K.limit ($24,500) — was hardcoded 23500 in withholding math
- [x] Test total: 301/301 passing; tsc clean on all touched files

### 1F. Remaining Phase 1
- [x] ToolPageLayout badges derived from rate constants (TAX_BADGES map: salary-uk,
      salary-us, stamp-duty, capital-gains-tax; "Last checked" = rates-module
      LAST_CHECKED). Inflation demoted to neutral "Last updated" badge (not tax-year data).
- [x] about.astro renders {UK_TAX_YEAR}/{US_TAX_YEAR}
- [x] Freshness guard: src/lib/rates-freshness.test.ts fails when tax year rolls over
      without a rates update or rates unchecked >400 days; wired as npm `prebuild` so
      every build runs it
- [x] CONTENT SWEEP — DONE (workflow: 5 clusters + independent verify + my follow-ups):
      - 17 UK salary scenarios → 2026/27 (student-loan figures recomputed; HICBC taper
        errors on 6 pages fixed; comparison-table errors on 40k/60k/100k pages fixed)
      - 15+3 US salary/401k scenarios → 2026 (all figures recomputed vs values table;
        narrative claims that 2026 thresholds invalidated were rewritten — e.g. $180k no
        longer crosses the SS cap)
      - 6 tool pages updated (salary-uk/salary-us worked examples recomputed; stamp-duty
        page had a garbled "wait, that's 15,000). Actually:" fragment + 4 stale pre-2025
        figures — fixed; capital-gains example 3 was wrong even under 2025 law — fixed)
      - 4 methodology pages updated; debt-payoff.md verified consistent with the FIXED
        rollover engine (no worked example to recompute)
      - Comparisons audit: 9 of 15 articles corrected (401k/IRA 2026 limits, dividend
        allowance £500 + Apr-2026 rates, pension access age, Cash-ISA-2027 caveats added,
        TCJA-expiry claim rewritten, Roth example recomputed)
      - My follow-ups on verifier flags: Scottish tax commentary on 5 scenario pages
        recomputed with verified 2026/27 Scottish bands (40k ~£65 more, 70k ~£1,950,
        85k ~£2,550, 95k ~£3,050, 100k ~£3,300/£30,732); snowball-vs-avalanche example
        recomputed with the fixed engine (now $1,634 vs $1,235, 23 vs 22 months, explicit
        minimums stated); retirement-savings all 3 worked examples recomputed with the
        engine ($1.64M/$1.4M/$3.8M — all three were wrong before); fixed-vs-variable
        remaining balance £213,800→£219,600; cash ISA rate table refreshed to mid-2026
        web-verified rates; salary-us overtime example (2.5x→1.5x math); retirement-age
        SS credit 132%→124%; IRS 2026 source URL corrected
      - Known deferred: InflationCalc CPI table ends at 2025 (engine data — add 2026
        estimate in a later pass); NLW £12.21 mention labelled 2025 (accurate as stated)
- [x] VERIFY: `npm test` 301/301 ✓; `npm run build` 197 pages 0 errors ✓ (freshness
      guard ran as prebuild ✓); dist grep 0 slash-less internal hrefs ✓; badges render
      "Updated for 2026/27 tax year · Last checked 2 July 2026" ✓; one JSON-LD
      breadcrumb per page ✓; consent-default-denied in every head ✓;
      `npx playwright test` 74/74 ✓ (fixed en route: 3 a11y violations — tablist
      structure + text-neutral-400 contrast in DecisionEngine, missing dark-mode
      override for .text-primary-700 badge text; plus de-flaked the HEIC test by
      scoping its locator to main)
- [x] **PHASE 1 COMPLETE — committed as c11d19c** (docs previously in 65e3a8a).
      Working tree clean except owner's untracked .docx audit files + screenshots/.

## PHASE 2 — Design Overhaul: "Precision Instrument" (NOT STARTED)

Sequence (each step ships independently; screenshots before/after for owner sign-off):
1. **Tokens:** ink #0E1116 / paper #FCFCFA palette in global.css @theme; teal sole accent;
   semantic tokens (bg-surface / text-ink / border-line) replacing the fragile 260-line
   `[data-theme=dark]` override list. Semantic number colors: gains = ink/green,
   costs = red; NEVER coral on positive money (currently take-home pay renders coral-red).
2. **Typography:** single grotesk (Inter Tight or Space Grotesk, self-host woff2) for
   everything; JetBrains Mono (subset to ~28KB) for every financial figure, table column,
   axis tick, result number; real type scale (14/16/20/28/40/56); drop Libre Baskerville.
3. **Core components:** SliderInput visual redo (16-18px thumb, 4-6px track, no overhang,
   value bubble on drag, 1px ring); one Badge component (2 variants max — currently 5+
   pill styles per page); buttons; cards 1px border no shadow; affiliate cards as quiet
   recommendation rows; logo wordmark re-set in brand face (current SVG is hand-traced,
   wobbly), currentColor for dark mode.
4. **Chart system:** shared chartTheme module for all 23 calculators — solid faint grid
   (no dashes), series1 teal 2px / series2 neutral-400, direct end-labels (no legend on
   2-series), neutral tooltip card with mono values, donut center KPIs ("78% kept"),
   annotated crossover markers ("interest overtakes your deposits in year 14"), 300ms
   count-up on headline numbers, dark-mode aware (current legend is illegible in dark).
5. **Page chrome:** compact light interior headers replace the 260-330px empty navy
   "aurora" bands (brand incoherence: homepage is light, interiors dark); solid 1px
   hairlines replace ALL gradient dividers; delete gradient-text utilities (.result-number,
   .stat-number); prose sections get sticky "On this page" TOC (kills dead right column).
6. **Calculator page recomposition:** marketing module diet — ONE email capture per page
   (kill either inline box or scroll bar); ResultAffiliate moves below the schedule table
   (currently interrupts results between buttons and chart); trim affiliate box; sticky
   summary-only results header (fixes nested-scrollbar trap + broken sticky thead);
   mobile sticky mini-result bar (real-time USP currently invisible on phones — results
   are 2 screens below inputs); fix ragged mobile button row (EmailResultsButton
   items-end); bottom-edge collision (cookie banner + scroll bar + FAB).
7. **Homepage:** restyle hero to new system; REPLACE Solar tab with "What's my take-home
   pay?" (UK + US — also restores the regressed UK prompt from Sprint 33A); "Open full
   calculator" becomes solid primary CTA (currently pale ghost pill outshone by cookie
   Accept); tabs get clickable affordance; below-fold sitemap-dump becomes designed rows
   with precomputed result previews in mono numerals ("£50K salary → £3,101/mo take-home"
   — values from docs/tax-values-2026.json); trust strip under hero (ACA credential →
   /about, "every calculator shows its formula" → methodology links, tax-year
   verification); differentiator proof strip ("44 calculators · results as you type ·
   no signup, no ads"); visible search box (index already exists); newsletter copy into
   its card. Keyworded H2 for discovery section + keyworded subhead line.
8. **CalculatorShell:** introduce shared shell (button row / sticky layout / PDF sections
   / getResults wiring) and migrate calculators as they're restyled — kills the 52.7%
   line duplication across 23 components. Incremental, 2-3 calculators per batch,
   E2E suite as safety net.
9. **Perf rides along:** CookieConsent → vanilla Astro inline script (drops React from
   ~150 content pages); DecisionEngine client:load → client:idle; recharts → React.lazy
   behind fixed-height container with shimmer (cuts calculator eager JS ~194KB gz →
   ~60-80KB); Turnstile script deferred until email form opened; nav SVG icons → sprite
   (40-70KB/page saved); Astro prefetch enabled; modulepreload hints; delete orphaned
   TTFs; subset JetBrains Mono.
- Dark mode re-verified per step (semantic tokens make it mostly automatic).
- Load `frontend-design` skill at the start of this phase.

## PHASE 3 — Content & SEO Restructure (NOT STARTED)

1. Merge 8-9 cannibal scenario/comparison pairs (identical slugs/keywords): keep the
   comparison, 301 the scenario twin via public/_redirects, remove from sitemap.
2. Consolidate ~70 doorway scenarios into interactive HUB pages (Decision 2):
   - "UK take-home pay by salary" hub: table £20K-£100K+ with anchors + embedded
     calculator; keep the strongest 3-4 individual salary pages (50K, 100K) enriched
   - US equivalent ($40K-$200K)
   - "Mortgage payments by amount" hub (150K-750K series → one page, rate-comparison table)
   - Investment-amount series → growth hub
   - 301 all retired variants; keep ~30 genuinely differentiated scenarios
3. E-E-A-T on everything that survives: byline block (full name, ACA, link to expanded
   author page w/ ICAEW+LinkedIn), datePublished/dateModified in content schemas AND
   rendered, Article + FAQPage JSON-LD on scenarios/comparisons, sources[] on comparisons.
4. Internal linking: nearest-neighbor related scenarios (salary ladders ±£5K), in-body
   links in comparisons (0 today), tool→scenario cluster links.
5. Meta rewrites: tool descriptions lead with target keyword (Sprint 17 over-stripped
   them); expand 18 stubby utility descriptions to 120-155 chars.
6. 5-8 NEW hand-written high-intent pages: pay-rise shock (UK+US), 60% tax trap,
   £100K childcare cliff, Plan 5 student loans explainer, stamp duty FTB 2026,
   PCP vs HP vs loan, CGT second home 2026/27 — with byline/schema/calculator links.

## PHASE 4 — Revenue Plumbing — **DEFERRED BY OWNER** (Decision 7)
(Single partner module, SubID tracking, scenario email capture, MailerLite drip,
affiliate applications — parked until owner wants traffic/revenue work.)

## PHASE 5 — Flagship (NOT STARTED, after 1-3)
- "UK Take-Home Truth Engine": tax + NI + student loans + salary sacrifice + childcare
  cliff + marginal-rate curve, shareable URL state. Debt-vs-invest calculator.
  URL state sharing on all calculators. Chart storytelling elaborations.

---

## SESSION HANDOFF STATE (update every session!)

**As of 2026-07-03: PHASE 1 + PHASE 2 COMPLETE AND LIVE — deploy verified on
www.calcrun.com (new design serving; 2026/27 badges live; 0 slash-less internal
links; immutable asset caching active; brand OG images serving; instant-answer
figures rendering). Deploys go GitHub → Cloudflare Workers Builds and take
~20-30 min to propagate.**
Commits: Phase 1 c11d19c · foundation 335f717 · wave 2 d13eed8 · responsive
efe10dd/3deebe8 · wave 3 47fc26c · wave 4 (perf/OG/footer/TOC finale) — pushed to
origin claude/master on owner instruction ("at the end of phase 2, update docs and
commit and push to live deploy"). Final gauntlet at deploy: 301 unit · 197-page
build · responsive sweep 0/588 · E2E 74/74.

RESUME POINT for a fresh session: next major work is **PHASE 3 (SEO restructure)**
— cannibal-pair 301s, doorway-scenario consolidation into hub pages, E-E-A-T
(bylines/dates/Article+FAQPage schema), internal-link ladders, meta rewrites, 5-8
new high-intent pages (see Phase 3 section above). Also available: the
post-deploy maintenance list at the end of the Phase 2 notes (CalculatorShell
dedup, lazy recharts, etc.). After deploy, owner should watch Cloudflare Pages
build status + GSC. Verify local state with: npm test, npm run build, then
`npx astro preview --port 4399` + `node scripts/qa-responsive-sweep.mjs` (exit 0).
Known transient: Playwright fill() on SliderInput text fields misbehaves
(controlled-input race — use pressSequentially in new specs; real typing is fine).
docs/design-system.md carries a supersession banner (old design — reference only).

PHASE 2 RUNNING NOTES:
- [x] Step 1-2 foundation COMMITTED (335f717): Space Grotesk variable (22KB) replaces
      Libre Baskerville for h1/h2 + font-serif token repointed; JetBrains Mono variable
      (40KB, replaces static regular) now sets EVERY financial figure via the global
      .tabular-nums rule — the signature; ink/paper neutral ramp + paper bg + border
      #E4E4E0 tokens; teal-only focus ring; gradient text (.result-number/.stat-number),
      gradient h2 rules + hr, gradient stat-highlight bg all flattened; font preloads
      updated. NOTE: public/fonts DMSans-Regular.ttf + LibreBaskerville-Bold.ttf are
      REQUIRED by the 4 OG endpoints (src/pages/og/*) at build time — do not delete;
      OG templates still on old brand type, queued for redesign this phase.
- [x] SliderInput visual redo: 16px thumb / 6px track / ring instead of shadow /
      44px touch target maintained (uncommitted)
- [x] useChartTheme extended into the full chart system (series1 teal / series2
      neutral / gain green / cost red / segments / mono axis font, light+dark) —
      application to the 23 calculators queued behind the color sweep
- [x] CLAUDE.md Design Quality Standards REWRITTEN for Precision Instrument (old
      gradient/dark-hero mandates banned)
- [⏳] Agent running: semantic money colors sweep (gains=green, costs=red, no coral
      on data) across src/components/tools/*.tsx (added success-50/100 tints;
      success-600 darkened to #14805A for AA on white)
- [x] Agent DONE: page chrome — all interior dark/aurora hero bands → compact light
      headers (tools/scenarios/comparisons indexes + detail templates, methodology,
      about, privacy/terms/disclosure/accessibility, embed); AuroraBackground.astro
      DELETED; gradient dividers in those pages → solid. Breadcrumb variant="dark"
      is now a dead code path (harmless).
- [NEW: OWNER REQUEST 2 Jul] RESPONSIVE QA GATE — all pages must work great on
      mobile/tablet/desktop. Tooling written (repo root, temp): .tmp-responsive-sweep.mjs
      (loads EVERY built page at 360/768/1280, reports horizontal overflow + offending
      elements + console errors → scratchpad responsive-report.json) and
      .tmp-shot-matrix.mjs (screenshot matrix of 14 representative pages × 3 viewports
      + dark subset for visual review). Run both after every major Phase 2 step; fix
      all overflow findings; visual-review the matrix via workflow agents. Known mobile
      items promoted into this gate: sticky mobile result bar (real-time results
      visible while dragging), staggered mobile button row fix (EmailResultsButton
      items-end), bottom-edge collision (cookie banner + scroll email bar + FAB),
      slim non-overlapping cookie banner.
- [x] RESPONSIVE GATE **PASSED (round 2 final)**: 0 findings / 588 checks — every
      page clean at 360/768/1280, zero page errors. Round-2 fixes: table-wrapper
      script now SKIPS React islands (moving island DOM pre-hydration caused React
      #418 on 3 pages); all 20 in-component calculator tables got explicit
      overflow-x-auto wrappers (9 added, 11 already present). QA tooling made
      PERMANENT: `scripts/qa-responsive-sweep.mjs` (exits 1 on findings — run after
      every major UI change) + `scripts/qa-shot-matrix.mjs` (visual review captures,
      pre-scrolls for scroll-reveal). Both need `npx astro preview --port 4399`
      serving dist first.
- [x] RESPONSIVE GATE ROUND 1:
      - Automated sweep: 0 findings / 588 checks (was 35 mobile overflows — fixed
        via table handling, pre overflow guard, methodology variable wrap)
      - Mobile sticky result mini-bar LIVE + verified end-to-end (appears in inputs
        area, live-updates on input, hides at headline; data-headline-result wired
        in all 23 calculators)
      - Cookie banner → slim single-line bar (bottom-left desktop); ScrollEmailBar
        waits for cookie consent; EmailResultsButton items-end stagger fixed
      - Visual review (3 agents, 50 shots) findings fixed: stat-card money clipping
        on ≤416px (cards stack + overflow-wrap backstop), half-width table
        regression (blanket CSS replaced with BaseLayout auto-wrapper: overflowing
        tables get .table-scroll box with right-fade cue, fitting tables keep
        full width), mortgage chart axis tick mush (preserveStartEnd + minTickGap)
      - FALSE ALARM understood: "3,500px dead blank" in full-page captures =
        scroll-reveal content at opacity-0 (screenshot artifact, real UX fine);
        matrix script now pre-scrolls to trigger reveals
- [ ] Visual-review mediums folded into upcoming sweeps: coral Subscribe/Get-tips
      CTAs + coral "Popular" pills + converter privacy box styled as alert (→ coral
      cleanup pass, one coral CTA max); prose-table figures not mono/semantic
      (→ marked-renderer or CSS pass with the chart/content polish); percentage
      calculator label/value collision; footer still navy (→ footer restyle with
      homepage); scenarios index length (→ Phase 3 hubs)
- [x] WAVE 3 COMPLETE (commit 47fc26c; verified 301 unit + 74 E2E + sweep 0/588):
      chart system on all 21 chart calculators (themed grid/axes/series, semantic
      colors, donut center KPIs, legends dropped on ≤2-series, crossover
      annotations on compound/investment-return, themed ChartTooltip); badge soup
      → one meta line + one tax badge; "How we calculate this →" text link;
      affiliate cards → quiet recommendation rows w/ disclosure ABOVE links
      (AffiliateLinks + ResultAffiliate, all tracking attrs preserved);
      ResultAffiliate moved to end of results panels; ONE email capture per tool
      page (inline EmailCapture removed, ScrollEmailBar stays); coral off all
      non-semantic UI (Subscribe buttons, Popular pills, privacy boxes, JWT
      success state, tip boxes, DecisionEngine bars); HOMEPAGE REBUILT: take-home
      tab (US/UK, live tax engines) replaces solar, solid teal CTA, tab hover
      affordance + chevrons, trust strip (ACA + verified tax years + formulas),
      "Instant answers" rows with build-time computed results in mono, newsletter
      card, hero microline (46 tools · no signup · no ads); NEW LOGO (type-set
      Space Grotesk wordmark + rising-spark mark). Note: charts agent was cut off
      by session limit mid-run; a finisher agent completed the remaining 8 files.
- [x] **WAVE 4 — PHASE 2 FINALE (complete):**
      - Nested-scrollbar trap fixed: lg:max-h/overflow-y removed from all 23
        results panels (sticky preserved for short panels)
      - PERF: CookieConsent → vanilla CookieBanner.astro (React REMOVED from all
        non-island pages — 0 astro-islands on content pages, ~62KB gz saved on
        ~150 pages); hero client:load → client:idle; Turnstile loads only when
        the email form opens; nav icons → SVG sprite (48 symbols once, 84 uses);
        Astro prefetch enabled (viewport strategy)
      - OG templates redesigned to brand: paper/ink/teal + spark-mark logo,
        static Space Grotesk 600 TTF instanced from the variable font via
        fonttools (public/fonts/SpaceGrotesk-SemiBold.ttf); shared
        src/pages/og/_brand.ts; scenario cards show the result figure
      - Footer restyled light (last legacy dark band gone; uses shared Logo)
      - "On this page" TOC right rail on xl+ tool pages (IntersectionObserver
        active-section highlight; hidden when <3 h2s)
      - Prose/markdown figures now mono via rehype plugin
        (src/lib/rehype-mono-figures.mjs) wrapping money/number tokens in
        table cells + strong stats with .tabular-nums
      - Table wrapper re-runs on document.fonts.ready (font-swap width race)
      - FINAL GAUNTLET at deploy: 301 unit ✓ · build 197 pages ✓ ·
        responsive sweep 0/588 ✓ · E2E 74/74 ✓
- **DEFERRED to post-deploy maintenance** (invisible refactors, regression risk
  shouldn't gate deploy): CalculatorShell consolidation (52.7% component
  duplication), lazy-loading recharts behind React.lazy (~194KB gz eager →
  ~70KB on calculator pages), modulepreload hints, JetBrains Mono subsetting,
  comparison-table component figures to mono (markdown ones done).

---

Earlier session log (Phase 1):

- Working tree: UNCOMMITTED code changes on `claude/master` implementing Phase 1A-1F
  (docs committed in 65e3a8a). Owner approved LOCAL checkpoint commits; PUSH IS HELD
  until owner reviews the design phase (push auto-deploys via Cloudflare Pages).
- Unit tests: 301/301 passing (incl. 62 new calculator-math tests + 3 freshness-guard).
- Background work in flight (session-bound): content-sweep workflow (5 cluster agents +
  verify pass) updating UK/US salary scenarios, tool pages, methodologies to 2026/27 /
  2026 values from docs/tax-values-2026.json, plus a comparisons accuracy audit.
  If session died mid-sweep: grep src/data for '2025/26' and '(2025)' to find
  unconverted files; re-run the sweep per Phase 1F task list.
- After sweep completes: (1) recompute the debt-payoff worked examples the agents were
  told to extract-and-report (methodologies/debt-payoff.md + comparisons/
  snowball-vs-avalanche-debt.md) against the FIXED debtPayoff engine via a temp vitest
  file; (2) npm test; (3) npm run build (prebuild freshness guard runs automatically);
  (4) grep dist for slash-less internal hrefs (expect 0); (5) npx playwright test
  (specs updated for trailing slashes); (6) fresh screenshots; (7) update this doc;
  (8) checkpoint commit "Phase 1: correctness + trust".
- Known debt (deliberate): 22 pre-existing tsc errors in unrelated files (pdfjs-dist v5
  types, satori casts, worker types, playwright imports); `astro check` OOMs
  (pre-existing) — `npm run check` now uses tsc instead.
- Live site is STALE (last deploy 20 Mar 2026) — everything here is local until pushed.
- Phase 2 (design) NOT started. Load the `frontend-design` skill first when starting it.
- Reference docs: `docs/review-2026-07.md` (full findings), `docs/tax-values-2026.json`
  (authoritative tables; regenerable via temp vitest importing the tax libs).
